import Header from "./components/Header/Header"

function App() {
  return (
    <div className="global-wrapper">
      <Header />
      {/* 
       * I cant get the modal to cover the whole space. Taking a step back and doing navbar again.
      <Routes>
        <Route path="/" element={<HomePage />}> </Route>
      </Routes> */}
    </div>
  )
}

export default App
